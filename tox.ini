[tox]
envlist = python3.10
skipsdist = true

[testenv]
deps =
    pytest
    mypy
    pylint
    numpy

    fake-bpy-module-3.1
    ; Alternative to fake-bpy-module:
    ; blender-stubs==3.10.*

commands =
    ; run tests
    pytest {posargs}
    ; run style tests
    python -m mypy ./mcblend
    pylint ./mcblend
